---
name: writing-plans
description: 写长期计划或复杂实施计划时使用；本项目默认转用 D:\codex-home\skills\planning-with-files\SKILL.md，避免第二套计划真相源。
---

# Writing Plans（计划写作路由）

本项目不复制 LumioAgent 的 `docs/plans/` 计划体系。用户说“长期计划”时，默认使用：

`D:\codex-home\skills\planning-with-files\SKILL.md`

## 使用规则

- 复杂任务、研究任务、超过 5 次工具调用或跨会话工作，优先用 planning-with-files。
- 计划文件放项目根目录，按该全局 skill 的模板维护。
- `.spec/tasks/` 只放轻量任务卡，不替代长期计划。
- `openspec/` 继续放正式 proposal/change/spec。

## 与 LumioAgent 差异

- 不默认写 `docs/plans/YYYY-MM-DD-*.md`。
- 不默认要求提交计划文档。
- 不默认创建或使用 worktree。

