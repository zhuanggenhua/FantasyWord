# 换装测试运行态复测清单

> 当前状态：Unity Editor 已可自动化连接；2026-07-06 已完成一次 PlayMode 结构审计。以下清单保留为后续回归入口。

## 复测前提

| 前提 | 通过标准 |
|---|---|
| Unity Editor 已打开当前项目 | `Temp/UnityBridge/heartbeat` 存在且时间新鲜 |
| 编译完成 | Editor 不在 compiling 状态，Console 没有 C# 编译错误 |
| 目标场景 | `Assets/Scenes/EquipmentSystemDemo.unity` |
| 目标角色 | 场景里的玩家 Animator 必须是 `EquipmentSystemDemoCharacter`，不能是 Canvas、对话气泡或 UI 对象 |

## 必测项目

| 编号 | 问题 | 操作 | 通过标准 |
|---|---|---|---|
| R1 | 按钮点了没用 | 点击角色、动作、方向、分类、装备槽 | 2026-07-06 已验证中文动作按钮可触发状态变化；后续新增按钮继续复测。 |
| R2 | 动作不切换 | 依次点击 `攻击 / 采集 / 砍伐 / 挖矿 / 待机 / 行走`，再检查 `SowingSeeds / Watering` | 2026-07-06 已验证真实点击切换；`SowingSeeds / Watering` 这类当前只命中交互图标的 Farm 动作不得用 `Harvest/Chopping/Mining/Idle` 冒充。 |
| R3 | 动作数量不止这点 | 滚动动作列表到底部 | 2026-07-06 真实 PlayMode 已验证 UI 显示 26 个动作项；其中 17 个真人身体帧动作可点击，9 个 Farm 交互键可见但标注 `无真人帧` 且不可点击，不用假动作冒充。 |
| R4 | 装备图标缺失 | 打开每个装备分类，查看右侧装备列表 | 2026-07-06 精确 PlayMode 已验证装备槽 `equipmentSlotCount=2`、`equipmentSlotIconMissing=0`；卸下槽和布衣都有图标。后续新增装备继续复测。 |
| R5 | 装备 UI 一排看不全 | 查看装备列表首行 | 2026-07-06 已验证装备网格为 4 列，格子 `108x96`；仍建议最终截图验收视觉观感。 |
| R6 | 非玩家混入 | 检查播放对象和动作来源 | 播放对象不是 UI、对话气泡、动物或怪物；动作库不出现 Monsters/Beasts/Farm_Animals/UI/Props/Shadows/Gif |
| R7 | 覆盖控制器 | 切换其它人形种族或对应覆盖控制器 | 覆盖控制器能播放同一真人动作集合；Farm 交互键若无真人帧，不应强行要求覆盖控制器映射 |
| R8 | 导入设置工具 | 打开 `Tools/Equipment System/MiniFantasy Import/` | “审计”菜单可直接运行；三个“应用”菜单会先弹确认，不会误点后直接批量改 `.meta` |

## 复测后回填

复测完成后必须更新：

- `docs/ai/bugs/换装测试问题验收表.md`
- 必要时更新 `docs/ai/bugs/换装测试动作来源表.md`
- 如导入设置仍异常，更新 `docs/ai/bugs/第三方素材meta变更审计.md`
