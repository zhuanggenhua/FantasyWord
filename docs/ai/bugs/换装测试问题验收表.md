# 换装测试问题验收表

> 范围：`Assets/Scenes/EquipmentSystemDemo.unity` 换装测试场景。
> 目标：每个用户反馈必须有状态、证据和下一步；不能再把“查了/改了/可能好了”混成完成。

## 当前状态

| 编号 | 用户反馈 | 当前状态 | 已有证据 / 已做改动 | 下一步验收 |
|---|---|---|---|---|
| 1 | 很多按钮点了没用 | 已修并有 PlayMode 点击证据 | 2026-07-06 PlayMode 审计显示按钮数 37，中文动作按钮可触发状态变化：`攻击 0->3`、`采集 3->10`、`砍伐 10->12`、`挖矿 12->13`、`待机 13->0`、`行走 0->1`。 | 后续新增按钮时继续用真实点击复测，不只看代码调用。 |
| 2 | 动作不切换 | 基础动作已修；Farm 无真人帧动作已禁用预览 | PlayMode 审计显示 `Attack / Harvest / Chopping / Mining` 可选且能切换；`SowingSeeds / Watering / Harvesting / TillingSoil / Digging / FillingBucket` 不可选，避免继续用 `Harvest` 冒充这些动作。 | 若要真实种植/浇水/耕地动作，必须补真人身体帧后再允许播放。 |
| 3 | 攻击动作缺失 | 已修并有资产证据 | 动作库含 `Attack`，控制器含 `Attack / Attack_NE / Attack_NW / Attack_SE / Attack_SW`。 | 无需再补同名攻击；只需后续确认其它攻击变体是否有真实人类素材。 |
| 4 | 动作数量不止这点，要主动查所有场景 | 已修并有真实 PlayMode 证据 | 当前 `AnimationType` 目录有 27 个 `.asset`，其中 26 个动作项；2026-07-06 真实 PlayMode 审计显示 `animationOptions=26`、`animationGridActiveChildren=26`。动作来源表已把人类正式动作、其它人形覆盖、Farm 交互键、动物/怪物/UI/特效排除项分开。 | 后续新增动作时继续回到 `EquipmentSystemDemo.unity` 真实 PlayMode 复测列表数量、滚动列表和点击行为。 |
| 5 | 种植、浇水、耕地等 Farm 动作要有 | 已锁定素材真相，待真人帧来源决定 | `Butchering / Digging / FillingBucket / Harvesting / Milking / Shearing / SowingSeeds / TillingSoil / Watering` 已在动作库中作为动作键；Farm 原包 `.anim` 引用的是 `ActionInProgress(16x16)` 交互图标，不是人物身体帧，当前不应放进玩家控制器当可播放身体动作。 | 若要真实种植/浇水/耕地动作，需要另找 MiniFantasy 人物身体帧或制作派生帧；否则只能作为玩法交互键，不能用其它身体动作假冒。 |
| 6 | 不要把非玩家、对话气泡 UI、动物怪物放进来 | 已修并有静态/运行态证据 | 场景中玩家 Animator 路径为 `EquipmentSystemDemoCharacter`，不在 `Canvas/RectTransform` 下；动作同步过滤了动物、怪物、对话/气泡、ActionInProgress 图标路径。 | 后续全场景动作来源表必须继续标明排除原因。 |
| 7 | 动画控制器位置、无连线、纯代码切换 | 已修并有证据，路径迁到 `Animations` | 控制器目标位置调整为：`Assets/GameData/EquipmentSystem/Animations/换装代码驱动状态机.controller`；2026-07-06 只读审计显示当前控制器 57 个状态、0 条连线、0 个参数。 | 继续保持无连线；新增真人动作只进状态，由代码 `Animator.Play` 切换。 |
| 8 | 其他种族用覆盖控制器 | 部分完成 | 项目侧覆盖控制器目标目录调整为：`Assets/GameData/EquipmentSystem/Animations/Overrides`；动作来源表已明确其它人形种族不新增动作类型，走同动作集合覆盖。 | 需要逐个验证覆盖控制器映射是否完整，不能只验人类。 |
| 9 | 装备连图都没显示 | 已修并有真实 PlayMode 证据 | 2026-07-06 精确 PlayMode 审计只统计装备槽图标，显示 `equipmentSlotCount=2`、`equipmentSlotIconMissing=0`；卸下槽使用 `EquipmentWorkbenchEmptySlotIcon`，布衣显示真实图标 `Craftable_Items_Icons_107`。 | 后续新增装备分类时继续复测空槽、真实装备图标和 fallback 图标。 |
| 10 | 装备 UI 一排看不全 | 已修并有 PlayMode 布局和图面证据 | 装备格改为 4 列，PlayMode 审计显示 `Equipment Grid columns=4`、格子 `108x96`、右侧视口 `446x340`；当前 GameView 结构验收显示右侧装备列表已填充，证据在 `test-results/evidence-image-validation/equipment-workbench-current/result.json`。 | 后续新增装备分类或更多装备项时，继续复测右侧滚动列表和图标裁切。 |
| 11 | 动作列表也是滚动列表 | 已修并有真实 PlayMode 证据 | 运行时代码已改为显示完整动作库，不再只过滤出可播放动作。2026-07-06 真实 PlayMode 审计显示 `动作 Scroll` 下 `Animation Grid children=26`、`animationGridActiveChildren=26`、`animationGridInteractableChildren=17`；9 个 Farm 交互键显示为 `·无真人帧` 且不可点击，`click_Watering=0->0` 证明不会误切到假动作。 | 如果以后补齐 Farm 真人身体帧，再把对应动作改为可播放并复测点击切换。 |
| 12 | 素材导入设置像是变了，是否要编辑器工具统一设置 Point 插值等 | 已有工具并已加安全门槛，待 Unity 编译验证 | 已新增 `docs/ai/bugs/第三方素材meta变更审计.md`。当前 `Assets/Art` 已跟踪 `.meta` 变更 396 个，其中 `spritePixelsToUnits` 121 个、`filterMode` 44 个、`isReadable` 329 个、`spriteMode` 49 个、`textureCompression` 345 个。项目已有 `MiniFantasyPixelImportTool`，本轮已给 3 个“应用”菜单加确认弹窗，避免误点后直接批量改第三方素材 `.meta`。 | Unity 打开后验证 Editor 编译；后续默认先用审计菜单，不直接应用全部设置。 |
| 13 | 你是不是动了素材 | 已审计，未回滚 | 审计表确认：`Assets/Art` 存在大量第三方素材 `.meta` 和新增素材包文件变化；本轮未执行回滚，因为可能撤掉素材来源、demo 场景和 GUID 闭包。 | 如要修导入设置，先走白名单审计工具；如要撤销第三方素材变更，需要用户确认具体范围。 |
| 14 | 运行态验收 | PlayMode 核心链路通过；布衣异常块已修正为“无可见服装层”状态 | 2026-07-06 真实 PlayMode 审计确认：`EquipmentSystemDemo.unity` 进入 PlayMode、目标对象是 `EquipmentSystemDemoCharacter`、场景未脏、动作列表 26 项、可播放 17 项、Farm 无真人帧项 9 项禁用且不误切、`click_Attack=0->3`、装备槽图标缺失数 0、装备列表 4 列。补充图面验收使用同一人类角色、Idle、方向 0，分别截取卸下与布衣状态；当前版本两张 1920x1080 真实 GameView 全图差异为 0，运行态状态显示 after 为人类、布衣已装备、`_EnableCloth=0`、`_ClothTex` 为空。结论：之前角色背后的异常块来自布衣错误引用精灵整帧，现已不再渲染；但布衣当前没有真实可见服装层，不能再把“出现差异”当成通过。证据在 `test-results/equipment-workbench-audit/latest-playmode-ui-structured/playmode-summary.txt`、`test-results/evidence-image-validation/equipment-workbench-outfit-fix-current/image-audit-v2.json` 和 `test-results/evidence-image-validation/equipment-workbench-outfit-fix-current/runtime-state-detailed.json`。 | 覆盖控制器仍需逐种族逐动作复测；Farm 真人身体帧仍需素材来源；布衣若要可见外观，需要补正确的人类服装层素材。 |

## 当前不能算完成的项

- Farm 真人动作：已证明 Farm 原包是交互图标动画，不是人物身体帧；不能算玩家动作完成。
- 覆盖控制器：还没逐种族逐动作验收。
- 导入设置工具：已有工具并已加确认保护，仍需 Unity 编译验证。
- 最终图面：当前已补同一人类角色“卸下 / 布衣”的 GameView 对照截图；布衣错误精灵整帧异常块已消失，当前布衣没有可见服装层。仍需后续覆盖到其它人形种族覆盖控制器。
