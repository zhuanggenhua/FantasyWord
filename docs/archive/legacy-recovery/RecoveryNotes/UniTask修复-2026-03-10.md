## UniTask 修复记录（2026-03-10）

### 问题现象
- Unity 编译报错 `CS2015`
- 报错文件位于 `Assets/Plugins/UniTask/Runtime`
- 典型报错为“二进制文件而非文本文件”

### 排查结论
- 当前项目内 `Assets/Plugins/UniTask/package.json` 版本为 `2.5.10`
- 扫描结果显示原目录下 `156` 个 `.cs` 文件中，大部分存在二进制污染，不适合逐个修复
- 该问题属于恢复目录内第三方插件文件损坏，不是业务脚本逻辑错误

### 处理方式
- 已将损坏包备份到项目外目录：
  - `C:\Gamedev\Unity\_temp\UniTask-broken-backup-20260310-0900`
- 已下载并解压官方 `UniTask 2.5.10` 源码包
- 已用官方同版本源码覆盖：
  - `C:\Gamedev\Unity\Project\FantasyWord\Assets\Plugins\UniTask`

### 修复后验证
- `Assets/Plugins/UniTask` 下 `.cs` 文件复查结果：
  - `TotalCs=156`
  - `UnreadableCs=0`
- 整个项目 `.cs` 文件复查结果：
  - `TotalCs=7440`
  - `UnreadableCs=0`

### 当前状态
- `CS2015` 对应的 `UniTask` 文本损坏已排除
- 当前环境未定位到可直接运行的 Unity Editor，因此尚未执行一次新的 Unity 批处理编译
- 下一步建议在本机用项目目标版本打开并触发重导入：
  - `ProjectSettings/ProjectVersion.txt:1`
  - 目标版本：`6000.3.10f1`

### 后续兼容修复
- 使用官方 `UniTask 2.5.10` 后，`UniTask.Yield()` 返回的是 `YieldAwaitable`
- `ZFrame` 旧代码中有一处把它直接塞进 `List<UniTask>`，会报：
  - `CS1503: 无法从 Cysharp.Threading.Tasks.YieldAwaitable 转换为 Cysharp.Threading.Tasks.UniTask`
- 已修复文件：
  - `Assets/Plugins/ZFrame/RunTime/Manager/UI/UIMgr.cs`
- 修复方式：
  - 将 `UniTask.Yield()` 改为 `UniTask.Yield().ToUniTask()`

### 如果重新打开后仍报错
- 优先把新的首批编译错误发出来
- 如果仍旧显示旧的 `CS2015`，再考虑清理 `Library/Bee` 与脚本编译缓存
- 然后继续恢复业务层缺失脚本和框架接口
