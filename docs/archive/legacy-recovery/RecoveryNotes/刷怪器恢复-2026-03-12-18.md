# 刷怪器恢复记录（2026-03-12-18）

## 本轮判定

- 处理文件：
  - `Assets/Scripts/Spawners/AMonsterSpawner.cs`
  - `Assets/Scripts/Spawners/MonsterSpawner.cs`
  - `Assets/Scripts/Spawners/MonsterAreaSpawner.cs`
- 判定结果：
  - 旧工程同名文件存在，但内容已损坏，不可直接复用
  - `Mythril2D` 参考同名文件完整，适合按同名骨架直接迁移
  - 当前工程缺失的是 persistence/save-load 宿主，而不是刷怪运行时主逻辑

## 本轮恢复策略

- 直接沿用参考版的：
  - `MonsterSpawn` 权重配置
  - 预刷怪逻辑
  - 刷怪冷却
  - 同屏数量上限
  - 总刷怪数限制
  - 点刷怪 / 区域刷怪两种定位方式
- 去掉当前工程没有的：
  - `Persistable`
  - `PersistableDataBlock`
  - `CharacterBaseDataBlock`
  - `OnSave` / `OnLoad`
  - `PersistenceSystem.InstantiateCustom`
- 改为当前工程可运行的宿主版本：
  - `Instantiate(prefab, position, rotation)` 直接生成怪物实例
  - 用 `List<SpawnedMonster>` 跟踪当前存活怪物
  - 每帧清理 `null` 或 `IsDead` 的条目，替代参考版依赖的 `destroyedEvent`

## 宿主补齐

- 为承接参考版的 `monster.SetLevel(...)` 调用：
  - 扩展 `Assets/Scripts/Entities/Characters/CharacterBase.cs`
  - 新增 `SetLevel(int)` 入口
- 当前没有补怪物成长公式：
  - `MonsterSheet` 目前只有奖励数据
  - 没有参考工程那种按等级驱动的 stats 数据
  - 因此本批只做真实等级同步，不伪造第二套成长底座

## 编译验证

- 批处理日志：
  - `RecoveryNotes/unity-batch-compile-20260312-23.log`
- 结果：
  - Unity 脚本编译通过

## 差集更新

- `missing-scripts-after-journal-current.txt`
  - 本轮前：`15`
  - 本轮后：`12`

## 剩余方向

1. 继续清 `CharacterStateBase` / `CharacterAnimState` / `CharacterTriggerState`
2. 继续清 `DashAbility` / `SummoningAbility`
3. 最后再收尾 `UISave*` / `UIMainMenu` 与编辑器脚本
