# 宿主装配兼容与 GUID 对齐（2026-03-12）

## 本轮目标

- 从“缺脚本恢复”切到“参考宿主场景 / prefab 可识别”
- 不先盲搬整包资源，先把参考 `M2DEngine.unity` / `Main Menu.unity` / `User Interface.prefab` 里的 Missing Script 压到最少

## 本轮完成

### 宿主兼容层

- 新增：
  - `Assets/Scripts/Audio/AudioChannel.cs`
  - `Assets/Scripts/Game/Systems/GameStateSystem.cs`
  - `Assets/Scripts/Game/Systems/NotificationSystem.cs`
  - `Assets/Scripts/UI/Menus/IUIMenu.cs`
  - `Assets/Scripts/Database/Items/EItemTransferType.cs`

### 兼容改造

- `Assets/Scripts/Game/Systems/AudioSystem.cs`
  - 改成兼容参考场景 `m_audioChannels.m_keys / m_values` 的序列化形状
  - 同时保留当前工程直接 `PlayOneShot` / `PlayLoop` / `Stop` 调用
- `Assets/Scripts/UI/Menus/AUIMenu.cs`
  - 薄实现 `IUIMenu`
- `Assets/Scripts/Game/GameManager.cs`
  - 新增 `GameStateSystem` / `NotificationSystem` 静态入口

### GUID 对齐

- 对齐到参考 / 旧工程的核心宿主脚本包括：
  - `AudioSystem` / `InputSystem` / `UISystem` / `MapSystem` / `TransitionSystem`
  - `GameFlagSystem` / `JournalSystem` / `PlayerSystem` / `SaveSystem`
  - `DialogueChannel` / `PlayerController` / `CommandTrigger`
  - `UIMainMenu` / `UISettings` / `UISaveFile`
  - `UICharacter` / `UIJournal` / `UIAbilities` / `UICraft`
  - `UIGameMenu` / `UIDeath` / `UISave` / `UIShop`
  - `UINavigationCursor` / `UIPlayerControllerFeedback`
  - `UIEventLog` / `CombatTextDisplay` / `FloatingTextPool`
  - `DisableInRelease`

## 依赖扫描结果

- `M2DEngine.unity`
  - 缺口从 `21` 降到 `8`
  - 剩余主要是：`CFG_Game.asset`、`PlayerInputActions.inputactions`、`User Interface.prefab`、`Devon.prefab`、`AC_Transition.controller`、`PersistenceSystem.cs`
- `Main Menu.unity`
  - 缺口从 `22` 降到 `12`
  - 剩余主要是：按钮 / 设置菜单 prefab、存档 asset、字体 / GUI sprite、`PlayerInputActions.inputactions`
- `User Interface.prefab`
  - 缺口从 `35` 降到 `20`
  - 当前主要卡在：`UIManager.cs`、`UIMenuManager.cs` 和一串菜单 / HUD prefab 资源

## 验证

- 使用 Unity Roslyn 复放：
  - `Library/Bee/artifacts/1900b0aEDbg.dag/Assembly-CSharp.codex-validate.rsp`
- 由于新增源码不会自动出现在旧 `rsp` 中，本轮追加了：
  - `AudioChannel.cs`
  - `GameStateSystem.cs`
  - `NotificationSystem.cs`
  - `IUIMenu.cs`
  - `EItemTransferType.cs`
- 结果：通过

## 当前结论

- 这轮之后，参考宿主的主要问题已经从 “Missing Script” 收缩成 “缺 asset / prefab / 少量宿主脚本”
- 下一步最合理的是：
  1. 评估是否补 `PersistenceSystem.cs`
  2. 再决定是否迁 `CFG_Game.asset` / `PlayerInputActions.inputactions` / `AC_Transition.controller`
  3. 单独处理 `UIManager.cs` / `UIMenuManager.cs` 与 `User Interface.prefab` 这条 UI 宿主链
