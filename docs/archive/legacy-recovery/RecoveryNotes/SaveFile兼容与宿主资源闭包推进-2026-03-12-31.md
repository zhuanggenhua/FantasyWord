# SaveFile 兼容与宿主资源闭包推进 - 2026-03-12

## 本轮目标
- 修正 `SaveFile` 的真实程序集边界问题，拿到可信 Roslyn 结论
- 继续压参考宿主 `M2DEngine.unity` / `Main Menu.unity` / `User Interface.prefab` 的直连缺口

## 关键动作
- 先运行 `ZFrame.rsp`，确认 `SaveFile.cs -> SaveDataBlock` 的跨程序集依赖在真实编译边界下必然失败
- 将 `SaveFile.m_content` 从 `SaveDataBlock` 改为 JSON 模板字符串，并在 `SaveSystem` 中新增 `ExtractSaveDataFromJson(...)`
- 重新运行 `ZFrame.rsp` 并通过
- 重新运行 `Assembly-CSharp.codex-validate.rsp`，显式追加本轮新增的 `Assets/Scripts` 文件并通过
- 新建最小资源资产：
  - `SF_Knight.asset`
  - `SF_Wizard.asset`
  - `SF_Archer.asset`
  - `AUDIO_ISFX_Quest_Started.asset`
  - `AUDIO_ISFX_Quest_Completed.asset`
  - `AUDIO_BGM_Title.asset`
- 对齐 UI 脚本 GUID：
  - `UINavigationTarget.cs`
  - `UIControllerButtonManager.cs`
  - `UISettingsChannelVolume.cs`
  - `UISettingsMasterVolume.cs`
- 迁入低依赖资源闭包：
  - `RPGSystem SDF.asset`
  - `SPS_GUI.png`
  - `Base Menu.prefab`
  - `Button.prefab`
  - `Settings Menu.prefab`
  - `UI Controller Button Manager.prefab`
  - 控制器 sprite libraries / sprites
  - `User Interface.prefab`
  - `Devon.prefab`

## 结果
- 可信编译结果：
  - `ZFrame.rsp` 通过
  - `Assembly-CSharp.codex-validate.rsp` + 追加新增源码 通过
- 参考宿主缺口：
  - `M2DEngine.unity`: `5 -> 3 -> 1`
  - `Main Menu.unity`: `12 -> 8 -> 3`
  - `User Interface.prefab`: `20 -> 17`

## 剩余判断
- `M2DEngine.unity` 剩余 1 个 `0000000000000000e000000000000000`，属于内置/占位型未知 GUID
- `Main Menu.unity` 剩余 3 个未知项中：
  - `357186adf88f47441beed107c9dbbe69` 只在参考场景文本中出现，参考树里找不到对应 `.meta`
  - `6a160d838ff8b4b4693ac20007e008c7` 来自旧版 `com.unity.2d.pixel-perfect` 包缓存
- `User Interface.prefab` 剩余 17 项已不再是脚本缺失，而是完整 UI prefab 链和 `UIManager.cs` / `UIMenuManager.cs` 宿主分叉问题

## 结论
- 当前高优先级“宿主可识别 + 可编译”目标已经明显收口
- 下一阶段如果继续推进，优先级应从“补脚本/补最小资产”切到“整批评估 UI prefab 链是否值得整体迁入”
