# 输入与资源绑定静态验证（2026-03-12）

## 本轮目标

- 不再继续补脚本数量，而是验证最后一批恢复代码的实际装配风险
- 重点检查：
  - `InputSystemGenerator` 的 fallback 日志是否干净
  - `InputSystem_Actions.inputactions` 的 map / action 实际结构
  - 最后恢复的状态类与 `MultiTrack` 是否已有资源侧引用
  - 当前仓内可见场景是否具备 gameplay 宿主实测条件

## 代码修正

- 已调整 `Assets/Scripts/GameSystem/Input/InputSystemGenerator.cs`
- 修正前：
  - `OpenGameMenu` / `Point` 这类 action 即使通过后备候选成功解析，也会先写一条 `Missing action ...`
- 修正后：
  - 只有在所有 fallback candidate 都失败时，才写最终缺失告警
  - fallback 成功时只保留真实的 fallback 提示

## 输入资产结论

- `Assets/InputSystem_Actions.inputactions` 当前只有两个 action map：
  - `Player`
  - `UI`
- `Player` map 当前包含：
  - `Move`
  - `Look`
  - `Attack`
  - `Interact`
  - `Crouch`
  - `Jump`
  - `Previous`
  - `Next`
  - `Sprint`
- `Player` map 不包含 `OpenGameMenu`
- 因此当前 gameplay 状态下的开菜单，仍主要依赖运行时代码中的原始输入兜底：
  - `Keyboard.escape`
  - `Gamepad.startButton`

## 资源绑定结论

- 已按 GUID 搜索以下脚本在 `.unity` / `.prefab` / `.asset` 中的引用：
  - `CharacterStateBase`
  - `CharacterAnimState`
  - `CharacterTriggerState`
  - `MultiTrack`
- 结果：
  - 当前可见资源中没有这些脚本的 GUID 引用
  - 说明这批恢复脚本目前仍停留在“源码可用”，尚未进入资源装配阶段

## 场景状态

- 当前仓内可见场景只有：
  - `Assets/Scenes/SampleScene.unity`
  - `Assets/Settings/Scenes/URP2DSceneTemplate.unity`
- 两者都仍是模板级场景
- 未发现当前恢复链路对应的 gameplay 宿主对象、`InputSystem` 挂载或 `PlayerInput` 资源绑定

## 编译校验

- 使用：
  - `Library/Bee/artifacts/1900b0aEDbg.dag/Assembly-CSharp.codex-validate.rsp`
  - Unity 自带 `dotnet.exe` + `csc.dll`
- 结果：
  - 退出码 `0`
  - 本轮警告清理后源码仍通过 C# 编译器校验

## 结论

1. 当前“缺脚本”阶段已经结束
2. 当前新的主风险不在源码，而在资源装配：
   - 输入宿主何时、在哪个场景或 prefab 中真正挂载
   - 状态类和 `MultiTrack` 何时接入 Animator / Timeline 资源
3. 如果下一轮继续推进，优先级应转为：
   - 恢复 gameplay 宿主场景/预制体
   - 再做真实运行时联调
