# 最后五个脚本恢复记录（2026-03-12）

## 本轮范围

- `Assets/Scripts/Entities/Characters/States/CharacterStateBase.cs`
- `Assets/Scripts/Entities/Characters/States/CharacterAnimState.cs`
- `Assets/Scripts/Entities/Characters/States/CharacterTriggerState.cs`
- `Assets/Scripts/GameSystem/Input/InputSystemGenerator.cs`
- `Assets/Scripts/MultiTrack.cs`

## 判定

- 旧工程中的同名源码已损坏，无法直接提取可用实现
- 当前可访问参考树中也没有这 5 个文件的可读同名源码
- 因此本轮按当前工程宿主恢复“可复用底座”，而不是补最小占位空壳

## 本轮完成

### 角色状态机

- 新增 `CharacterStateBase`
  - 作为 Animator `StateMachineBehaviour` 使用
  - 负责角色查找、模式切换、动作锁与进出状态停移动
- 新增 `CharacterAnimState`
  - 复用当前项目已有的 `MessageData` / `IAnimationMessageReceiver`
  - 支持 enter / exit / threshold 消息派发
- 新增 `CharacterTriggerState`
  - 统一处理进入/退出状态时的 trigger 与 bool 设置/重置

### 输入系统对齐

- 新增 `InputSystemGenerator`
- 用它接管运行时对 action map / action 名的解析，修正当前 `.inputactions` 资产里 `Player/UI` 与运行时代码里 `Gameplay/UI` 的不匹配
- 已补 alias：
  - `FireAbility1 -> Attack`
  - `FireAbility2 -> Previous`
  - `FireAbility3 -> Next`
  - `FireAbility4 -> Jump`
  - `FireAbility5 -> Crouch`
  - `OpenGameMenu -> UI.Cancel`
  - `Point -> Look / UI.Point`

### Timeline 轨道

- 新增 `MultiTrack`
- 恢复为可用 Timeline 轨道，而不是空文件
- 当前结构包含 clip + mixer，可向绑定对象派发 enter / exit `MessageData`

## 宿主改动

- `Assets/Scripts/Entities/Characters/CharacterBase.cs`
  - 增加 `SetMode(CharacterMode)`
- `Assets/Scripts/Game/Systems/InputSystem.cs`
  - 改为通过 `InputSystemGenerator` 解析实际 map / action
- `Assets/Scripts/Controllers/PlayerController.cs`
  - 打开游戏菜单改走 `InputSystemGenerator.WasOpenGameMenuPressed(...)`
  - 保留 `Keyboard.escape` / `Gamepad.startButton` 兜底

## 差集结果

- `RecoveryNotes/missing-scripts-after-journal-current.txt`
- 当前文件大小：`0`
- 结论：路径差集已从 `5` 清到 `0`

## 验证

### Unity batch compile

- 日志：
  - `RecoveryNotes/unity-batch-compile-20260312-29.log`
  - `RecoveryNotes/unity-batch-compile-20260312-30.log`
- 结果：
  - 未出现新的 `error CS` 脚本编译错误
  - 失败原因为当前沙箱环境中的 `LocalLow/Unity` 权限失败和 UPM IPC 连接失败
  - 因此这两次失败不能当作脚本源码失败结论

### Unity Roslyn 校验

- 使用：
  - `Library/Bee/artifacts/1900b0aEDbg.dag/Assembly-CSharp.codex-validate.rsp`
  - `C:\Gamedev\Unity\Editor\6000.3.10f1\Editor\Data\NetCoreRuntime\dotnet.exe`
  - `C:\Gamedev\Unity\Editor\6000.3.10f1\Editor\Data\DotNetSdkRoslyn\csc.dll`
- 结果：
  - `csc.dll` 退出码为 `0`
  - 当前源码层通过 C# 编译器校验

## 残余风险

1. 需要在真实 Unity 编辑器会话里做一次运行时实测
2. 重点验证：
   - Animator 状态 enter / exit / threshold 消息
   - `InputSystemGenerator` 的 alias 是否覆盖所有输入入口
   - `MultiTrack` 的 Timeline binding 对象是否都实现了消息接收
