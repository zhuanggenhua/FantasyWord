# third_party

`third_party/puerts` is vendored from `E:/github/puerts` so CI and temporary Unity projects can reference PuerTS through local UPM `file:` dependencies without relying on another checkout.

Refresh it with:

```powershell
Packages/puerts-unity-mcp/Tools~/vendor-puerts.ps1
```

