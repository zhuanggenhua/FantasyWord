{
  "name": "scene-create",
  "title": "Scene / Create",
  "description": "Create new scene in the project assets. Use 'scene-list-opened' tool to list all opened scenes after creation.",
  "parameters": [
    {
      "name": "path",
      "type": "string",
      "description": "Path to the scene file. Should end with \".unity\" extension.",
      "required": "true"
    },
    {
      "name": "newSceneSetup",
      "type": "string",
      "default": "1",
      "required": "false",
      "description": "Scene setup. ENUM: \"DefaultGameObjects\" (camera+light), \"EmptyScene\". Default: \"DefaultGameObjects\""
    },
    {
      "name": "newSceneMode",
      "type": "string",
      "default": "0",
      "required": "false",
      "description": "Scene mode. ENUM: \"Single\" (close other scenes), \"Additive\" (keep other scenes). Default: \"Single\""
    }
  ],
  "hints": {
    "notes": [
      "scene-create 属于场景受保护命令；若当前打开场景栈可能存在生成场景 dirty，调用时必须显式传 bridgeSceneDirtyPolicy",
      "若创建前只需要清掉已知生成场景残留，优先传 bridgeSceneDirtyPolicy=discard-generated；若本次创建结果要正式落盘，按流程显式保存"
    ]
  }
}
