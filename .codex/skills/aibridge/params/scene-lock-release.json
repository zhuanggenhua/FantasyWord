{
  "name": "scene-lock-release",
  "title": "Bridge / Scene Lock / Release",
  "description": "Release a previously acquired AIBridge scene lock.",
  "parameters": [
    {
      "name": "token",
      "type": "string",
      "description": "The lock token returned by scene-lock-acquire.",
      "required": "true"
    }
  ]
}
