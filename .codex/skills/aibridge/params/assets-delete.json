{
  "name": "assets-delete",
  "title": "Assets / Delete",
  "description": "Delete the assets at paths from the project. Does AssetDatabase.Refresh() at the end. Use 'assets-find' tool to find assets before deleting.",
  "parameters": [
    {
      "name": "paths",
      "type": "array",
      "description": "Array of asset paths to delete. Example: [\"Assets/Materials/Mat.mat\"]",
      "required": "true"
    }
  ],
  "hints": {
    "commonMistakes": [
      "Parameter is paths (array), NOT assetPath (string)"
    ]
  }
}
