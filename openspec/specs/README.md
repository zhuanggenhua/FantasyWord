# OpenSpec Specs

这里放已经归档生效的 `FantasyWord` 正式 capability specs。

当前已归档 capability：

- `foundation-runtime`
- `ability-authoring-foundation`
- `equipment-visual-workbench`

新能力应先在 `openspec/changes/<change-id>/specs/<capability>/spec.md` 中提出 delta，通过验证和归档后再进入本目录。
