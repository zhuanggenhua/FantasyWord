# OpenSpec Changes

这里放 `FantasyWord` 的活跃 OpenSpec change。

## 当前状态

- `define-fantasyword-foundation-framework` 已于 `2026-06-22` 归档到 `archive/2026-06-22-define-fantasyword-foundation-framework/`。
- `formalize-equipment-visual-workbench` 已于 `2026-06-22` 归档到 `archive/2026-06-22-formalize-equipment-visual-workbench/`。
- `complete-composite-sandbox-character-runtime` 已于 `2026-06-22` 归档到 `archive/2026-06-22-complete-composite-sandbox-character-runtime/`。
- `define-skill-authoring-workbench` 已于 `2026-06-27` 归档到 `archive/2026-06-27-define-skill-authoring-workbench/`，其正式能力名已收口为 `ability-authoring-foundation`，不再沿用 `workbench` 术语。
- 当前活跃 change：`plan-core-framework-roadmap`。
- 后续新增 change 时，目录名使用动词短语，例如 `define-fantasyword-foundation-framework`。
- 不复制 `dark-corridor` 的横版动作、Swordmaster、内功芯片或 Celeste 平台动作 change。

## 历史首个基础 change

- `define-fantasyword-foundation-framework`
- 范围：俯视角开放世界基础框架、插件接入边界、素材/装备表现候选、目录合同、验证口径和小规模联机预留轴。
