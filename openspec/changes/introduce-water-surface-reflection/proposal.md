# Proposal: introduce-water-surface-reflection

## Why

当前移动测试场景已经有动画水，后续开放世界还会把水默认作为可进入、可游泳区域。水面倒影如果只按常见教程做成“反射相机翻转画面 + 水面 Shader 采样”，很容易和本项目的实际需求冲突：

- 水是动画 RuleTile，不是静态平面。
- 项目是俯视角像素游戏，倒影需要像素感、轻量、可裁剪，而不是写实水面反射。
- 移动端需要控制额外渲染成本，不能每块水域或每条河流单独创建反射相机。
- 水默认可游泳，游泳动画已经自己处理“只显示上半身”的视觉，不应再由倒影方案重复处理身体遮罩。
- 水面倒影需要按距离和对象状态分级：近处有明显倒影，中距离弱化，远处只保留水面动画或暗色波动。

因此需要独立 change，把水面倒影定义为“水域表现系统”的一部分，而不是孤立 Shader 特效。该 change 只提出方案、规范和实施任务，不在提案阶段修改代码、材质、场景或渲染配置。

## Project Tier

长期项目。水面倒影会影响地图 Tilemap、URP 2D 渲染、角色表现、游泳状态、移动端性能和后续开放世界区域加载，不能作为单个测试场景的临时效果脚本处理。

## Current State Lock

- **问题对象**：动画水 Tilemap、可游泳水域判断、倒影捕获、倒影合成材质、移动测试场景中的水层。
- **真相来源**：`ClickMoveTest` 场景现有水层、`LakeGrass.asset` 动画规则瓦片、当前 URP 2D Renderer 配置、Unity 6 URP 2D Renderer 文档、用户提供的 B 站水面倒影教程。
- **目标入口**：水面视觉仍由现有动画水 Tilemap 承担；水域进入/游泳判断由正式水域状态入口承担；每个可倒影对象由反射代理组件管理；全部代理由一个场景级反射系统通过一台共享相机和一张低分辨率纹理捕获。
- **验收口径**：在当前移动测试场景中，角色可以进入水域并保持游泳动画逻辑，岸边树木/建筑/岸上角色在近水处有像素风倒影，远处倒影自动弱化或关闭，移动端成本可控。

## Scope

本 change 包含：

1. 定义水默认可游泳的开放世界规则，不把水 Tilemap 继续当作整块实体阻挡。
2. 定义水域状态入口，统一服务游泳、涟漪、倒影淡出和后续水体交互。
3. 保留现有动画水 RuleTile 作者流程，不复制第二套手工水面 Tilemap。
4. 为可倒影对象建立反射代理组，并使用一张共享低分辨率纹理捕获这些代理。
5. 定义近、中、远三个倒影质量层级，并允许最低档完全关闭倒影。
6. 定义水材质合成动画水、倒影纹理、水像素 Mask、轻微像素扰动和边缘扰动衰减。
7. 定义游泳中玩家倒影规则：不使用普通站立完整倒影，默认关闭或只保留极弱上半身波动影。
8. 约束场景级反射系统、代理 Layer、xBRZ、Camera Sorting Layer Texture、共享相机和 URP 2D Renderer Feature 的职责边界。
9. 兼容角色主体换装 Shader 和动态武器 SpriteRenderer，不把玩家倒影简化为单 Sprite。

## Out Of Scope

- 游泳动画本身、上半身显示、身体遮罩和游泳动作状态机。
- 真实 3D 水面、法线海浪、折射深度、物理浮力和潜水系统。
- 每个水域独立反射相机。
- 河流流速模拟、船只、漂浮物、完整水体生态。
- 多层地形桥洞/桥面导航；如水域与桥洞相交，按对应地形 change 另行处理。
- 端到端截图或视觉素材出图；实施阶段再按用户要求验收。

## Responsibility Verdict

| 职责 | 候选来源 | 正式 owner | 本次吸收什么 | 本次明确不吸收什么 | 验证入口 |
| --- | --- | --- | --- | --- | --- |
| 水面作者数据 | 动画 RuleTile、第二套水面 Mesh、自研水域编辑器 | 现有动画水 Tilemap + 后续水域状态入口 | 保留 RuleTile 动画水；补充水域状态/Mask | 复制第二套视觉水；用 Sprite 名称推断玩法水域 | `ClickMoveTest` 水层 |
| 水域进入判断 | Tilemap 实体碰撞、Trigger、导航地表类型 | 正式水域状态入口 | 水默认可游泳；岸线/障碍单独阻挡 | 把整块水 Tilemap 当墙；把倒影系统当游泳判断 owner | 角色进入水域测试 |
| 倒影几何与对象帧 | 手工子物体、运行时代理、全屏纹理翻转 | WaterReflectionCaster2D 管理的 Reflection Proxy 组 | 围绕脚底锚点翻转、垂直压缩、45 度偏斜；同步主体、装备和武器 Renderer | 每帧按名称扫描子物体；只复制主 Sprite 导致装备缺失 | 玩家、树木和建筑代理检查 |
| 倒影注册与粗剔除 | 跨场景单例、每水体管理器、场景级服务 | 场景级 WaterReflectionSystem | OnEnable/OnDisable 注册；按可见水域和代理 AABB 做空间粗剔除 | 全局玩法单例；向每个水材质上传全部对象位置数组 | 可见代理数量和 CPU 更新检查 |
| 倒影捕获 | 每水域相机、单共享相机、URP 2D Renderer Feature、Camera Sorting Layer Texture | 一台共享正交捕获相机 + 一张共享低分辨率 RT | 只捕获 WaterReflectionProxy Layer；使用独立 Renderer2D 隔离 xBRZ | 每条河/湖单独相机；每对象 RenderTexture；首版强制迁移 Renderer Feature | Frame Debugger 与 RT 内容检查 |
| 倒影对象筛选 | Sorting Layer、LayerMask、材质标签、对象组件 | WaterReflectionCaster2D + WaterReflectionProxy Layer | 只让明确注册的角色、树木、建筑上层等生成代理 | 捕获 UI、地表、整张屏幕、所有 Tilemap | 代理注册和捕获纹理检查 |
| 远近质量分级 | 物体到水域距离、相机距离、屏幕位置、全局质量档 | WaterReflectionSystem + 每代理属性 | 近水完整代理，中距离缩短/弱化，远处关闭；低端档关闭共享捕获 | 所有水面同成本、同强度反射；Shader 遍历对象数组 | 移动端性能测试 |
| 游泳中玩家倒影 | 普通完整倒影、弱化倒影、关闭倒影 | 角色水域状态 + 倒影对象过滤 | 游泳中玩家默认不显示完整站立倒影，可选极弱上半身波动影 | 重复实现游泳身体遮罩；把游泳动画拆给水 Shader 处理 | 游泳状态视觉验收 |
| 水材质合成 | Shader Graph、手写 URP Shader、后处理 | 水面材质/Shader | 动画水、倒影纹理、Mask、轻微扰动和距离淡出 | 写实 Fresnel 海面；全屏后处理替代水面材质 | 材质样例场景 |
| 像素风稳定性 | xBRZ 后处理、点采样 RT、像素网格对齐 | 水面倒影渲染配置 | 倒影纹理低分辨率、点采样或受控滤波、像素步进扰动 | 对倒影 RT 重复 xBRZ 导致糊边或重采样闪烁 | 放大截图核对 |

## Reference Matrix

| 参考 | 证据等级 | 命中职责 | 可吸收内容 | 不作为依据的内容 |
| --- | --- | --- | --- | --- |
| 用户提供 B 站教程；转录学习资料见 `references/bilibili-water-reflection-study.md` | 已完整转录，入门教程级 | 反射相机、RenderTexture、水 Shader 合成、UV 噪声扰动、边缘衰减 | 吸收共享 RT、水面合成、小幅 UV 扰动和边缘衰减 | 不采用每水域独立相机；不把普通纹理翻转当最终 45 度俯视角规则 |
| 公开实现对比；见 `references/water-reflection-industry-research.md` | 已读取公开源码和官方文档 | 对象代理、Stencil/Mask、每水体相机、移动端 SSPR | 吸收对象反射代理、脚底锚点翻转、45 度偏斜、共享 RT 和移动端降级 | 不照搬 Unity 5 脚本、Built-in Shader、每水体相机或 3D Compute SSPR |
| Unity 6 URP 2D Renderer 文档 | 官方文档 | 2D Renderer Feature、Render Graph、Camera Sorting Layer Texture | 确认共享纹理、降采样和后续 Renderer Feature 替换能力 | 不把屏幕排序层截取当长期倒影对象筛选真相；不为技术先进强制首版上 Renderer Feature |
| 当前 `ClickMoveTest` 水层 | 当前工程事实 | 动画水、碰撞、水域入口 | 保留动画水 RuleTile；拆分水域检测与岸线/障碍阻挡 | 继续让水层非 Trigger 碰撞阻止开放世界游泳 |
| 当前角色游泳设定 | 用户确认 | 游泳中玩家显示 | 游泳动画已处理只显示上半身，本 change 不重复做身体遮罩 | 用倒影系统驱动游泳动画或遮挡身体 |

## Distance-Based Reflection Policy

首批同时按“对象反射范围到最近可见水域的距离”和“对象到主相机焦点的距离”分级。前者决定是否应该产生倒影，后者决定质量预算；具体阈值在实施阶段用场景和设备调参：

| 等级 | 适用区域 | 倒影表现 | 成本策略 |
| --- | --- | --- | --- |
| 近景 | 代理 AABB 与可见水域相交，且位于相机主要交互区域 | 明显像素倒影，带轻微水波扰动和透明度衰减 | 启用完整 Proxy，使用共享 RT 采样 |
| 中景 | 仍可能投影到水面，但离水或离相机更远 | 倒影变暗、变短、垂直压缩更强，细节弱化 | 降低 Proxy alpha/长度，不新增局部相机 |
| 远景 | 代理范围不再与可见水域相交，或超出质量距离 | 不显示动态倒影，只保留动画水自身明暗变化 | 关闭 Proxy，不进入共享捕获 |
| 低端设备 | 移动端低质量档 | 关闭动态倒影，保留动画水和涟漪 | 不分配或低频更新倒影 RT |

## Sequencing

1. 建立 WaterReflectionCaster2D 和一个静态单 Sprite 代理，验证脚底锚点翻转、垂直压缩和 45 度偏斜。
2. 建立场景级 WaterReflectionSystem、WaterReflectionProxy Layer、一台共享捕获相机和一张低分辨率 RT。
3. 建立水面合成材质和水像素 Mask，验证代理只在真实水像素中显示。
4. 接入玩家主体换装 Shader 和动态武器 Renderer，保证完整角色外观进入代理组。
5. 接入水域状态：游泳动画继续由现有表现负责，游泳中的玩家不做完整站立倒影。
6. 增加可见水域空间粗剔除、近中远代理质量和低端关闭档。
7. 用独立 Renderer2D 隔离 xBRZ，并通过 Frame Debugger 和移动端性能数据验收。
8. 只有在共享相机成为已测量瓶颈时，再评估 Renderer Feature 内部替换；替换不得改变 Caster、Proxy、Mask 和水材质合同。

## Acceptance Direction

- 动画水 Tilemap 继续按现有 RuleTile 动画显示，不需要复制第二套视觉水。
- 大多数水域默认可游泳，水面本身不再整体阻挡角色。
- 岸线、石头、悬崖和地图边界仍可作为独立障碍阻挡移动。
- 岸上角色、树木和建筑在近景水面有可读的像素风倒影。
- 游泳中的玩家不出现完整站立倒影。
- 中远景水面倒影自然弱化，不因全屏水体导致移动端明显过载。
- 低质量档可以关闭动态倒影而不破坏水面动画和游泳表现。
- 倒影捕获不会把 UI、整张地表或无关 Tilemap 渲染进反射纹理。
- 玩家倒影包含主体换装结果和当前显示的武器，不退化成裸主体单 Sprite。
- 开放世界对象数量不会形成传给水 Shader 的固定长度位置数组。
- 多片水域共享同一捕获纹理和同一个场景级反射系统，不产生每水体相机。
